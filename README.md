# incremental-game

A simple game that I gradually add more features to.

How to Run
Start the Backend: Open a terminal in backend/ and run:

mvn spring-boot:run
Wait for "Started App in X seconds".

Start the Frontend: Open a terminal in frontend/ and run:

npm run dev
